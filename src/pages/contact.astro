---
// src/pages/contact.astro
import Header from "../components/Header.astro";
---

<Header />

<main class="bg-gray-100 text-gray-900">
  <section class="bg-white py-20">
    <div class="container mx-auto text-center">
      <h1 class="text-4xl font-bold text-gray-800 mb-4">Contacto</h1>
      <p class="text-lg text-gray-600 mb-8">
        Si deseas ponerte en contacto conmigo, por favor llena el siguiente
        formulario o envíame un correo.
      </p>
    </div>
  </section>

  <section class="py-20">
    <div class="container mx-auto">
      <form class="bg-white p-6 rounded-lg shadow-md max-w-lg mx-auto">
        <div class="mb-4">
          <label for="name" class="block text-gray-700 font-bold mb-2"
            >Nombre</label
          >
          <input
            type="text"
            id="name"
            class="w-full px-4 py-2 border rounded-lg"
            placeholder="Tu nombre"
          />
        </div>
        <div class="mb-4">
          <label for="email" class="block text-gray-700 font-bold mb-2"
            >Correo Electrónico</label
          >
          <input
            type="email"
            id="email"
            class="w-full px-4 py-2 border rounded-lg"
            placeholder="Tu correo"
          />
        </div>
        <div class="mb-4">
          <label for="message" class="block text-gray-700 font-bold mb-2"
            >Mensaje</label
          >
          <textarea
            id="message"
            class="w-full px-4 py-2 border rounded-lg"
            rows="4"
            placeholder="Tu mensaje"></textarea>
        </div>
        <button
          type="submit"
          class="w-full bg-blue-500 text-white px-6 py-3 rounded hover:bg-blue-600"
        >
          Enviar
        </button>
      </form>
    </div>
  </section>
</main>
